{
  "name": "met-up",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.2.0",
    "@material-ui/styles": "^4.2.0",
    "@reach/router": "^1.2.1",
    "@types/jest": "24.0.15",
    "@types/node": "12.6.2",
    "@types/react": "16.8.23",
    "@types/react-dom": "16.8.4",
    "axios": "^0.19.0",
    "date-fns": "^1.30.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "typescript": "3.5.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "clean:modules": "rimraf node_modules",
    "clean:yarn": "rimraf ./yarn.lock",
    "clean:all": "run-s clean:yarn clean:modules",
    "refresh": "run-s clean:modules && yarn install",
    "regenerate": "yarn install && yarn clean:all && yarn install"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.6.3"
  }
}
